# byte-to-bite
StateBite is a session-based restaurant chatbot API built with Node.js and TypeScript. It utilizes a state machine pattern to manage automated food orders via a text interface using device tracking instead of logins. Features include dynamic menu selection, automated cart management, state validation, and secure Paystack payment integration.
